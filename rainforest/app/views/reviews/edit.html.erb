<%= render 'form', product: @review.product, review: @review %>

<%= link_to("Delete", product_path(@product, @review), method: :delete, data: {confirm: "Are you sure you want to delete this review?"})%>
