<%= render 'form', product: @review.product, review: @review %>

<%= link_to("Delete", "/products/#{@product.id}/reviews/#{@review.id}", method: :delete, data: {confirm: "Are you sure you want to delete this product?"})%>
